package data;

import java.util.List;

import ninja.NinjaTest;

import org.junit.Assert;
import org.junit.Test;

import com.google.inject.Injector;

public class RepositoryTest extends NinjaTest {
	
	/*@Test
	public void testBeatRepsitoryOverview() {
		
		// this is the application guice injector
        Injector injector = getInjector();

        BeatRepository beatRepo = injector.getInstance(BeatRepository.class);
        Assert.assertNotNull(beatRepo);
        
        List<OverviewBeat> overview = beatRepo.getBeatsOverview(13);
        Assert.assertNotNull(overview);
        
	}
	*/
}
